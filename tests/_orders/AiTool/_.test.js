/*
 * Imports define the run order for this category; add each new test in its correct position.
 *
 * The file writes tool rows of its own and reads no agent's bindings, so it neither depends on
 * another category nor disturbs one.
 */
import './AiTool.js'
